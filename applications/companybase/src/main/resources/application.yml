spring:

  datasource:
    url: jdbc:mysql://localhost:3306/company?useSSL=false
    username: root

  jpa:
    generate-ddl: true
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect

  mvc.view:
    prefix: /WEB-INF/
    suffix: .jsp

logging.level:
  root: WARN

employees.ms.url: http://localhost:8081/employees
clients.ms.url: http://localhost:8081/clients
